{
  "name": "@tether/types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos TypeScript compartidos entre apps y backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
