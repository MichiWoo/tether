{
  "name": "@tether/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "DTOs, eventos WebSocket y modelos compartidos del protocolo Tether",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
